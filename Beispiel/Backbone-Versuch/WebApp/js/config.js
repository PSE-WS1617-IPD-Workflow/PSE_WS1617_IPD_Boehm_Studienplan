var templateUrl = "templates/";
/*var templates = {
    wrapper : "wrapper.html";
}
// foreach tempalte do below...
// Add wrapper template
require(["text!" + templateUrl + "wrapper.html"], function (wrapperTemplate) {
    "use strict";
    alert(wrapperTemplate);
    AppTemplateRegistry.addTemplate("wrapper", wrapperTemplate);
});*/